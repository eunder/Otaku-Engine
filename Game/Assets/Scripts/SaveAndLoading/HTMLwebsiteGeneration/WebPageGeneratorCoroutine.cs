using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WebPageGeneratorCoroutine : MonoBehaviour
{

    //prefabs that get generated (main room, hallway door pieces, doors, etc...)


    //set the skybox color
    //just make the floor one big collider(for now?) (just worry about the spawn order and space between spawned elements ATM!)

    //loop for image link: if there is an image that is also a link... spawn a door and use the image of the link ass the thumbnail

    //loop for doors: every door faces the opposite direction(door 1 faces the other way, door 2 faces the other way...)

    //loop for images: just use the first lone image on a webpage(atm) as the center image in the room

   //  IEnumerator LoadWebsite()
   //  {


   //  }





}
